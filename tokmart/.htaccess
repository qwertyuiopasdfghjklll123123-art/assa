Options -Indexes
RewriteEngine On

# Friendly root: "/" already serves index.php by default on Apache.
DirectoryIndex index.php

<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
</IfModule>
